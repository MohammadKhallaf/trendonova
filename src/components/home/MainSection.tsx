const MainSection = () => {
  return (
    <main className="h-screen flex-grow border-x px-3">
      <header className="flex flex-row justify-between items-baseline">
        <h2 className="text-3xl font-bold">المنشورات</h2>
        <div>إضافة منششور</div>
      </header>
    </main>
  );
};

export default MainSection;
